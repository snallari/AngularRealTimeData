<div class=container>
	<ul class="heros">
		<li *ngFor="let hero of heroes" [class.selected]="hero===selectedHero"
			(click)="onSelect(hero)"><span class="badge">{{hero.id}}</span>{{hero.name}}
		</li>
	</ul>

	<app-herodetails [hero]="selectedHero"></app-herodetails>
</div>